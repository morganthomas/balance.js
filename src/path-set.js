/*

Path sets are an efficient way of representing finite sets of paths (see path.js).

arrayToPathSet(paths)
  Takes an array of paths and returns the set of those paths.

pathSetContains(set, path);
  Returns true if the given path set contains the given path.

pathSetToArray(set)
  Returns an array of the paths in the given path set.

*/
